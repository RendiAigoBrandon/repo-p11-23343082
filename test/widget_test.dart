import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Dummy test', (WidgetTester tester) async {
    // Aplikasi telah diubah menjadi Repository Pattern, jadi test counter dihapus.
    expect(true, true);
  });
}
